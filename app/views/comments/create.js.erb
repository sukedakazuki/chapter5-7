$('.comment-textarea').val("")
$('.comments-counter').html("<%= j(render 'comments/counter', book: @comment.book) %>")
$('.comments-index').html("<%= j(render 'comments/index', book: @comment.book) %>")